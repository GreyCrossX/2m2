[pytest]
# point pytest at your tests folder
testpaths = app/tests

# add repo root to sys.path so "services.*" imports work
pythonpath = .

# register the asyncio marker to silence warnings (and for pytest-asyncio)
markers =
    asyncio: mark a test as running with asyncio
